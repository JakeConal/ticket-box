apiVersion: v2
name: monitoring
description: Prometheus + Grafana + kube-state-metrics for TicketBox cluster and api observability (dev/staging only)
type: application
version: 0.1.0
appVersion: "v3.13.2"
