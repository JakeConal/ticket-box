apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app.kubernetes.io/name: web
    app.kubernetes.io/part-of: ticketbox-app
spec:
  selector:
    app.kubernetes.io/name: web
  ports:
    - name: http
      port: {{ .Values.web.port }}
      targetPort: http
